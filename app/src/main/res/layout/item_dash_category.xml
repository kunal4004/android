<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:padding="@dimen/ten_dp">

    <androidx.cardview.widget.CardView
        app:cardElevation="0dp"
        app:cardCornerRadius="@dimen/twenty_four_dp"
        android:id="@+id/imgCategoryCard"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:padding="@dimen/ten_dp"
            android:id="@+id/imgCategory"
            android:background="@color/color_F3F3F3"
            android:layout_width="@dimen/fourty_dp"
            android:layout_height="@dimen/fourty_dp" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/txtCategoryName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/ten_dp"
        android:fontFamily="@font/myriad_pro_regular"
        android:maxWidth="@dimen/seventy_dp"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="@dimen/twelve_sp"
        app:layout_constraintEnd_toEndOf="@id/imgCategoryCard"
        app:layout_constraintStart_toStartOf="@id/imgCategoryCard"
        app:layout_constraintTop_toBottomOf="@id/imgCategoryCard"
        tools:text="On Promotion" />

</androidx.constraintlayout.widget.ConstraintLayout>